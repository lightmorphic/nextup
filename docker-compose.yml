services:
  nextup:
    image: ghcr.io/fosscharlie/nextup:latest
    container_name: nextup
    restart: unless-stopped
    ports:
      - "4090:8080"
    volumes:
      - /opt/nextup:/data
    environment:
      # Hours between automatic refreshes of tracked shows.
      NEXTUP_SYNC_HOURS: "12"
